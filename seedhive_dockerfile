FROM kliew/hive-dev
MAINTAINER Alastair Paragas <alastairparagas@gmail.com>

# Add Initialization Scripts
ADD ./hive/init.sql /hive_init/init.sql

# Add Data files
ADD ./data/csv /data/

# On startup, execute Hive initialization script
CMD hive -h hive -f /hive_init/init.sql
